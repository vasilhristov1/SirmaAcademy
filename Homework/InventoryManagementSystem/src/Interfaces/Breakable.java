package Interfaces;

// Interface Breakable which indicates the breakable items
public interface Breakable {
    boolean isBreakable(); // function to return if an item is breakable
    void handleBreakage(); // function to handle the breakage of a breakable item
}
